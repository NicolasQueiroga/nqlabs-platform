# Cilium cluster override — nqlabs-production
# Use with: -f infrastructure/networking/cilium/values.yaml -f clusters/nqlabs-production/cilium/values.yaml

k8sServiceHost: 10.0.30.9
k8sServicePort: 6443

# 3-node HA cluster on Intel NUCs.
operator:
  replicas: 2
