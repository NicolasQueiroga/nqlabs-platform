# Cilium cluster override — nqlabs-production
# Use with: -f infrastructure/networking/cilium/values.yaml -f clusters/nqlabs-production/cilium/values.yaml

k8sServiceHost: 192.168.15.33
k8sServicePort: 6443

# Single-node production VM today. Raise to 2 only after multiple schedulable
# nodes exist on the NUC/HA cluster.
operator:
  replicas: 1
