# Pyroscope — central continuous profiling store.
# Chart: grafana/pyroscope 2.1.0

pyroscope:
  replicaCount: 1
  disableSelfProfile: true
  extraArgs:
    log.level: info
    pyroscopedb.max-block-duration: 30m
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      memory: 1Gi
  podSecurityContext:
    # Talos + local-path subPath bind mounts block non-root writes to the
    # metastore subPath even with fsGroup. Scoped to monitoring namespace.
    fsGroup: 0
    runAsUser: 0
    runAsNonRoot: false
    seccompProfile:
      type: RuntimeDefault
  securityContext:
    runAsUser: 0
    runAsNonRoot: false
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
  persistence:
    enabled: true
    storageClassName: local-path
    accessModes:
      - ReadWriteOnce
    size: 20Gi

minio:
  enabled: false

serviceMonitor:
  enabled: true
  namespaceSelector:
    matchNames:
      - monitoring
  labels:
    release: kube-prometheus-stack

alloy:
  enabled: true
  global:
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 10001
      runAsGroup: 10001
      fsGroup: 10001
      seccompProfile:
        type: RuntimeDefault
  alloy:
    securityContext:
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      seccompProfile:
        type: RuntimeDefault
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        memory: 256Mi
  configReloader:
    securityContext:
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      seccompProfile:
        type: RuntimeDefault
    resources:
      requests:
        cpu: 10m
        memory: 50Mi
      limits:
        memory: 128Mi
