# OTel Collector HTTPRoute removed — internal-only service, no external exposure.
# Anyone on the gateway could inject traces without auth.
# Tempo has no HTTPRoute (accessed via Grafana datasource, not directly exposed).
