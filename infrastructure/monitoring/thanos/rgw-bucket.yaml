# Provision a dedicated Thanos bucket on the internal Ceph RGW object store.
# The OBC generates credentials that are used in the objstore secret below.
---
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: thanos-rgw
  namespace: monitoring
  labels:
    app.kubernetes.io/name: thanos
    app.kubernetes.io/component: objstore
    app.kubernetes.io/part-of: nqlabs-platform
spec:
  bucketName: thanos
  storageClassName: ceph-bucket
---
# Thanos object-store config pointing at the internal Ceph RGW endpoint.
# Credentials are the OBC-generated S3 user for the thanos bucket.
# If the OBC is reprovisioned (new user), update these values.
apiVersion: v1
kind: Secret
metadata:
  name: thanos-objstore
  namespace: monitoring
  labels:
    app.kubernetes.io/name: thanos
    app.kubernetes.io/component: objstore
    app.kubernetes.io/part-of: nqlabs-platform
stringData:
  objstore.yml: |
    type: S3
    config:
      bucket: thanos
      endpoint: rook-ceph-rgw-platform-object-store.rook-ceph.svc:80
      access_key: UJPLS2J8SEP6C4Y7VAPB
      secret_key: 6eHsGcIg8js5lChoU95DSs7Sq9AKDUjbIHSK8Hjv
      insecure: true
      signature_version2: false
